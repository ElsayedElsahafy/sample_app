<li>
	<%= gravatar_for user %>
	<%= link_to user.name , user %>
	<% if current_user.admin? && !current_user?(user) %>
 |  <%= link_to 'Delete' , user , method: :Delete , data: {confirm: "you are sure?"}%>
    <%end%>
</li>